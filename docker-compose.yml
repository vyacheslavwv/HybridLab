version: '3.8'

services:
  frontend:
    build: ./hybrid-lab
    ports:
      - "3000:3000"
    restart: always

  backend:
    build: ./metrics-backend
    ports:
      - "8000:8000" 
    restart: always
